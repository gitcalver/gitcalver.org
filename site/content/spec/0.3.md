---
title: "Specification"
description:
  "GitCalVer specification—version format, branch model, deterministic
  interfaces, and publication contract"
# Copyright © 2026 Michael Shields. SPDX-License-Identifier: CC-BY-4.0
---

**Version**: 0.3

## Abstract

GitCalVer derives version numbers deterministically from git history. Each
commit on one selected first-parent chain maps to a unique, strictly increasing
version based on the commit’s UTC committer date and the size of its date
cohort—the commits reachable from it, through any parent, that share that date.

Calculation is local and offline. Publication may refresh remote state and claim
an immutable tag, resolving the possibility that independent clones calculate
the same candidate before either publishes it.

## Terminology

- The **selected branch** is the branch whose first-parent history defines
  versions.
- The **selected chain** is the selected branch tip followed repeatedly through
  first parents.
- The **target** is the commit being calculated. It is `HEAD` when no explicit
  revision is supplied.
- A **date block** is a maximal consecutive run of selected-chain commits with
  the same UTC committer date.
- The **date cohort** of a commit is the set of commits reachable from it,
  through any parent, whose UTC committer date equals its own. The commit is a
  member of its own date cohort.
- A **canonical tag** is a caller-defined tag prefix followed by a clean
  GitCalVer version.

## Version format

### Base format

```text
YYYYMMDD.N
```

Where:

- `YYYYMMDD` is the Gregorian UTC date of the commit’s committer timestamp,
  using a four-digit year, two-digit month, and two-digit day.
- `N` is a positive integer without leading zeroes. It is the size of the
  target’s date cohort: the count of commits reachable from the target, through
  any parent, whose UTC committer date equals the target’s.

For example:

| Commit | Committer date (UTC) | Version      |
| ------ | -------------------- | ------------ |
| `e1`   | 2026-04-10 09:00:00  | `20260410.1` |
| `e2`   | 2026-04-10 14:30:00  | `20260410.2` |
| `e3`   | 2026-04-10 17:00:00  | `20260410.3` |
| `e4`   | 2026-04-11 10:00:00  | `20260411.1` |
| `e5`   | 2026-04-11 11:00:00  | `20260411.2` |

For a history with no merges into the selected chain, this count is identical to
0.2’s; the two definitions diverge only when a merge brings same-date commits
onto the chain.

### Prefix

A caller MAY prepend a single-line literal prefix. The default prefix is empty.

| Use case                   | Prefix | Example         |
| -------------------------- | ------ | --------------- |
| Base form                  | empty  | `20260412.3`    |
| Initial or unstable SemVer | `0.`   | `0.20260412.3`  |
| Initial or unstable Go tag | `v0.`  | `v0.20260412.3` |

The prefix is not part of the base version and implementations MUST NOT infer
one. A nonempty prefix MUST NOT contain a newline.

Projects maintaining incompatible release lines MUST use distinct caller-managed
major prefixes or distinct package namespaces. The `0.` prefix denotes an
initial or unstable line; it is not a universal compatibility boundary.

## Sparse sequences

Because `N` counts a whole date cohort instead of a position on the first-parent
chain, a single merge can add more than one commit’s worth of `N` at once. The
resulting sequence of assigned versions is not dense:

| Commit  | Committer date (UTC)                            | Date cohort size | Version       |
| ------- | ----------------------------------------------- | ---------------- | ------------- |
| `m4`    | 2026-04-10                                      | 4                | `20260410.4`  |
| `merge` | 2026-04-10 (merges 16 same-day feature commits) | 21               | `20260410.21` |

A merge that brings in `k` same-date commits increases the date cohort, and
therefore `N`, by `k` plus one for the merge commit itself. `20260410.5` through
`20260410.20` were never assigned to any commit. Reverse lookup for a value in
that range MUST report that the version was not found. It MUST NOT return the
nearest lower or higher commit.

<figure id="fig-1" tabindex="0" aria-describedby="horizontal-scroll-instructions" data-horizontal-scroll="true">
<svg role="img" width="620" height="230" viewBox="0 0 620 230" aria-labelledby="fig-1-title fig-1-desc" style="font-family:'IBM Plex Mono',monospace">
<title id="fig-1-title">A merge that jumps N from 4 to 21 in one step</title>
<desc id="fig-1-desc">Four main-branch commits carrying N 1 through 4 are followed by a merge that brings in sixteen same-day feature commits plus the merge commit itself, so N jumps to 21. Versions 5 through 20 for that date were never assigned to any commit, and reverse lookup for any of them must report that the version was not found.</desc>
<defs>
<marker id="fig3-arrow" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
<path d="M0,0 L10,5 L0,10 Z" fill="var(--border-strong)"/>
</marker>
</defs>
<path d="M110,55 L80,55" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig3-arrow)"/>
<path d="M180,55 L150,55" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig3-arrow)"/>
<path d="M250,55 L220,55" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig3-arrow)"/>
<path d="M520,72 L390,110" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig3-arrow)"/>
<path d="M540,89 L280,55" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig3-arrow)"/>
<text x="440" y="102" font-size="10.5" fill="var(--text-soft)">1st parent</text>
<text x="450" y="70" font-size="10.5" fill="var(--text-soft)">2nd parent</text>
<rect x="20" y="39" width="60" height="32" rx="6" fill="var(--count-soft)" stroke="var(--count)" stroke-width="1.5"/>
<text x="50" y="60" text-anchor="middle" font-size="13" fill="var(--text)">m1</text>
<text x="50" y="26" text-anchor="middle" font-size="10.5" fill="var(--text-soft)">N=1</text>
<rect x="90" y="39" width="60" height="32" rx="6" fill="var(--count-soft)" stroke="var(--count)" stroke-width="1.5"/>
<text x="120" y="60" text-anchor="middle" font-size="13" fill="var(--text)">m2</text>
<text x="120" y="26" text-anchor="middle" font-size="10.5" fill="var(--text-soft)">N=2</text>
<rect x="160" y="39" width="60" height="32" rx="6" fill="var(--count-soft)" stroke="var(--count)" stroke-width="1.5"/>
<text x="190" y="60" text-anchor="middle" font-size="13" fill="var(--text)">m3</text>
<text x="190" y="26" text-anchor="middle" font-size="10.5" fill="var(--text-soft)">N=3</text>
<rect x="230" y="39" width="60" height="32" rx="6" fill="var(--count-soft)" stroke="var(--count)" stroke-width="1.5"/>
<text x="260" y="60" text-anchor="middle" font-size="13" fill="var(--text)">m4</text>
<text x="260" y="26" text-anchor="middle" font-size="10.5" fill="var(--text-soft)">N=4</text>
<rect x="330" y="94" width="150" height="34" rx="8" fill="var(--count-soft)" stroke="var(--count)" stroke-width="1.5" stroke-dasharray="5 3"/>
<text x="405" y="115" text-anchor="middle" font-size="12" fill="var(--text)">batch: 16 same-day commits</text>
<rect x="510" y="55" width="60" height="34" rx="6" fill="var(--count-soft)" stroke="var(--count)" stroke-width="1.5"/>
<text x="540" y="76" text-anchor="middle" font-size="13" fill="var(--text)">M</text>
<text x="540" y="41" text-anchor="middle" font-size="10.5" fill="var(--text-soft)">N=21</text>
<line x1="30" y1="185" x2="590" y2="185" stroke="var(--border-strong)" stroke-width="1.5"/>
<line x1="30" y1="178" x2="30" y2="192" stroke="var(--border-strong)" stroke-width="1.5"/>
<text x="30" y="170" text-anchor="middle" font-size="11" fill="var(--text)">1</text>
<line x1="90" y1="178" x2="90" y2="192" stroke="var(--border-strong)" stroke-width="1.5"/>
<text x="90" y="170" text-anchor="middle" font-size="11" fill="var(--text)">2</text>
<line x1="150" y1="178" x2="150" y2="192" stroke="var(--border-strong)" stroke-width="1.5"/>
<text x="150" y="170" text-anchor="middle" font-size="11" fill="var(--text)">3</text>
<line x1="210" y1="178" x2="210" y2="192" stroke="var(--border-strong)" stroke-width="1.5"/>
<text x="210" y="170" text-anchor="middle" font-size="11" fill="var(--text)">4</text>
<line x1="210" y1="185" x2="530" y2="185" stroke="var(--error)" stroke-width="2" stroke-dasharray="1 5" stroke-linecap="round"/>
<text x="370" y="205" text-anchor="middle" font-size="11" fill="var(--error)">5-20: no commit; reverse lookup MUST report not found</text>
<line x1="530" y1="178" x2="530" y2="192" stroke="var(--border-strong)" stroke-width="1.5"/>
<text x="530" y="170" text-anchor="middle" font-size="11" fill="var(--text)">21</text>
</svg>
<figcaption>Figure 1. A merge that brings in several same-date commits jumps N by more than one—here, 16 feature-branch commits plus the merge itself push N from 4 to 21 in a single selected-chain step. <code>YYYYMMDD.5</code> through <code>YYYYMMDD.20</code> were never assigned to any commit; reverse lookup for a value in that range MUST report that the version was not found, never the nearest commit on either side.</figcaption>
</figure>

## Guarantees and prerequisites

Within one fixed, complete selected chain, where no commit reachable from any
chain commit has a UTC committer date later than a commit that can reach it:

1. Every commit maps to exactly one clean base version.
2. Every clean base version maps back to at most one commit. The mapping need
   not be onto: a syntactically valid `YYYYMMDD.N` MAY correspond to no commit
   (see [Sparse sequences](#sparse-sequences)).
3. Versions strictly increase from parent to child.
4. The selected branch tip’s version never decreases as the branch advances by
   any combination of new commits and fast-forwards, regardless of which parent
   a merge records as first.

Within a date’s cohort, `N` strictly increases with each selected-chain step.
Across a date boundary, `YYYYMMDD` increases and therefore the whole version
increases regardless of the previous count.

<figure id="fig-2" tabindex="0" aria-describedby="horizontal-scroll-instructions" data-horizontal-scroll="true">
<svg role="img" width="580" height="370" viewBox="0 0 580 370" aria-labelledby="fig-2-title fig-2-desc" style="font-family:'IBM Plex Mono',monospace">
<title id="fig-2-title">The incident topology, scored two ways</title>
<desc id="fig-2-desc">A shared commit graph where four main-branch commits are merged into a feature branch and the merge is fast-forwarded back onto main. Panel A shows 0.2’s first-parent-only count dropping from 4 to 3. Panel B shows 0.3’s date-cohort count rising from 4 to 7 on the exact same graph, because every commit stays reachable.</desc>
<defs>
<marker id="fig1-arrow" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
<path d="M0,0 L10,5 L0,10 Z" fill="var(--border-strong)"/>
</marker>
</defs>
<text x="16" y="16" font-size="11.5" fill="var(--text-soft)">Seven same-date commits; Panel A and Panel B disagree on how many count</text>
<path d="M110,55 L80,55" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig1-arrow)"/>
<path d="M200,55 L170,55" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig1-arrow)"/>
<path d="M290,55 L260,55" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig1-arrow)"/>
<path d="M200,145 L170,145" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig1-arrow)"/>
<path d="M110,145 L95,145" stroke="var(--border-strong)" stroke-width="1.5" stroke-dasharray="2 3" fill="none"/>
<text x="72" y="149" font-size="14" fill="var(--text-soft)">...</text>
<path d="M380,95 L250,124" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig1-arrow)"/>
<path d="M410,79 L320,66" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig1-arrow)"/>
<text x="252" y="118" font-size="10.5" fill="var(--text-soft)">1st parent</text>
<text x="345" y="76" font-size="10.5" fill="var(--text-soft)">2nd parent</text>
<rect x="20" y="34" width="60" height="32" rx="6" fill="var(--bg-sunk)" stroke="var(--border-strong)" stroke-width="1.5"/>
<text x="50" y="55" text-anchor="middle" font-size="13" fill="var(--text)">C1</text>
<rect x="110" y="34" width="60" height="32" rx="6" fill="var(--bg-sunk)" stroke="var(--border-strong)" stroke-width="1.5"/>
<text x="140" y="55" text-anchor="middle" font-size="13" fill="var(--text)">C2</text>
<rect x="200" y="34" width="60" height="32" rx="6" fill="var(--bg-sunk)" stroke="var(--border-strong)" stroke-width="1.5"/>
<text x="230" y="55" text-anchor="middle" font-size="13" fill="var(--text)">C3</text>
<rect x="290" y="34" width="60" height="32" rx="6" fill="var(--bg-sunk)" stroke="var(--border-strong)" stroke-width="1.5"/>
<text x="320" y="55" text-anchor="middle" font-size="13" fill="var(--text)">C4</text>
<rect x="110" y="124" width="60" height="32" rx="6" fill="var(--bg-sunk)" stroke="var(--border-strong)" stroke-width="1.5"/>
<text x="140" y="145" text-anchor="middle" font-size="13" fill="var(--text)">F1</text>
<rect x="200" y="124" width="60" height="32" rx="6" fill="var(--bg-sunk)" stroke="var(--border-strong)" stroke-width="1.5"/>
<text x="230" y="145" text-anchor="middle" font-size="13" fill="var(--text)">F2</text>
<rect x="380" y="79" width="60" height="32" rx="6" fill="var(--bg-sunk)" stroke="var(--border-strong)" stroke-width="1.5"/>
<text x="410" y="100" text-anchor="middle" font-size="13" fill="var(--text)">M</text>
<text x="16" y="192" font-size="13" font-weight="600" fill="var(--text)">Panel A
— 0.2 (position in date block, first-parent chain only)</text>
<rect x="16" y="204" width="64" height="26" rx="13" fill="var(--count-soft)" stroke="var(--count)"/>
<text x="48" y="221" text-anchor="middle" font-size="12" fill="var(--text)">F1=1</text>
<path d="M84,217 L98,217" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig1-arrow)"/>
<rect x="98" y="204" width="64" height="26" rx="13" fill="var(--count-soft)" stroke="var(--count)"/>
<text x="130" y="221" text-anchor="middle" font-size="12" fill="var(--text)">F2=2</text>
<path d="M166,217 L180,217" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig1-arrow)"/>
<rect x="180" y="204" width="64" height="26" rx="13" fill="var(--count-soft)" stroke="var(--count)"/>
<text x="212" y="221" text-anchor="middle" font-size="12" fill="var(--text)">M=3</text>
<rect x="280" y="204" width="250" height="26" rx="13" fill="none" stroke="var(--border)" stroke-dasharray="3 3"/>
<text x="405" y="221" text-anchor="middle" font-size="11.5" fill="var(--text-soft)">C1,
C2, C3, C4 — not counted</text>
<text x="16" y="252" font-size="13" fill="var(--text)">Before the merge, C4’s own count was 4.&#160;<tspan fill="var(--error)" font-weight="600">4 to 3: decrease</tspan></text>
<text x="16" y="282" font-size="13" font-weight="600" fill="var(--text)">Panel B
— 0.3 (date cohort, all parents)</text>
<rect x="16" y="294" width="58" height="26" rx="13" fill="var(--count-soft)" stroke="var(--count)"/>
<text x="45" y="311" text-anchor="middle" font-size="11.5" fill="var(--text)">C1=1</text>
<path d="M74,307 L88,307" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig1-arrow)"/>
<rect x="88" y="294" width="58" height="26" rx="13" fill="var(--count-soft)" stroke="var(--count)"/>
<text x="117" y="311" text-anchor="middle" font-size="11.5" fill="var(--text)">C2=2</text>
<path d="M146,307 L160,307" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig1-arrow)"/>
<rect x="160" y="294" width="58" height="26" rx="13" fill="var(--count-soft)" stroke="var(--count)"/>
<text x="189" y="311" text-anchor="middle" font-size="11.5" fill="var(--text)">C3=3</text>
<path d="M218,307 L232,307" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig1-arrow)"/>
<rect x="232" y="294" width="58" height="26" rx="13" fill="var(--count-soft)" stroke="var(--count)"/>
<text x="261" y="311" text-anchor="middle" font-size="11.5" fill="var(--text)">C4=4</text>
<path d="M290,307 L304,307" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig1-arrow)"/>
<rect x="304" y="294" width="58" height="26" rx="13" fill="var(--count-soft)" stroke="var(--count)"/>
<text x="333" y="311" text-anchor="middle" font-size="11.5" fill="var(--text)">F1=5</text>
<path d="M362,307 L376,307" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig1-arrow)"/>
<rect x="376" y="294" width="58" height="26" rx="13" fill="var(--count-soft)" stroke="var(--count)"/>
<text x="405" y="311" text-anchor="middle" font-size="11.5" fill="var(--text)">F2=6</text>
<path d="M434,307 L448,307" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig1-arrow)"/>
<rect x="448" y="294" width="58" height="26" rx="13" fill="var(--count-soft)" stroke="var(--count)"/>
<text x="477" y="311" text-anchor="middle" font-size="11.5" fill="var(--text)">M=7</text>
<text x="16" y="345" font-size="13" fill="var(--text)">Before the merge, C4’s own count was 4.&#160;<tspan fill="var(--count)" font-weight="600">4 to 7: increase</tspan></text></svg>
<figcaption>Figure 2. The incident topology: merging the selected branch into a feature branch, then fast-forwarding the selected branch onto the merge, reparents the selected branch’s own commits behind a different first parent. Panel A shows why 0.2’s first-parent-only position count can decrease (4 to 3) even though nothing was rewritten. Panel B shows why 0.3’s date-cohort count cannot (4 to 7): every commit that used to count still does, because it’s still reachable, just through a different parent. Illustrative numbers; the real incident showed the same pattern at a larger scale—see <a href="/spec/0.2">the 0.2 erratum</a>.</figcaption>
</figure>

### Proof sketch

**Proof sketch.** Let `reach(x)` be the set of commits reachable from `x`
through any parent, including `x` itself. If `p` is a parent of `x` (first or
otherwise), then `reach(p)` is a subset of `reach(x)`, and `x` is not itself in
`reach(p)` (the commit graph has no cycles).

_Uniqueness._ When `p` is `x`’s first parent and `date(p) = date(x)`, the
date-cohort predicate is identical for both sets, so `x`’s date cohort contains
`p`’s date cohort plus `x` itself: `N(x)` is at least `N(p) + 1`. This holds
regardless of which parent Git recorded as first, because it only requires `p`
to be _a_ parent of `x`. Combined with `YYYYMMDD` strictly increasing across
date boundaries, no two selected-chain commits share a `(YYYYMMDD, N)` pair.

_Monotonicity under any DAG growth._ A branch ref only ever advances to a
descendant. Let `T₁` be the selected chain’s tip before an advance and `T₂` its
tip after—so `T₁` is reachable from `T₂`. If `date(T₂) > date(T₁)`, the version
increases because the date segment is compared first. If `date(T₂) = date(T₁)`,
then `reach(T₁)` is a subset of `reach(T₂)` exactly as above, giving an `N(T₂)`
that is at least `N(T₁) + 1`. Crucially, this argument never used _which_ parent
of `T₂` leads to `T₁`—only that one does. So it holds even when a merge changes
which chain `T₁`’s own ancestors are reachable through, including the case where
a merge-into-feature-branch followed by a fast-forward reparents earlier
selected-chain commits behind a different first parent. `T₁` and everything in
its date cohort remain reachable from `T₂` either way, so they are still
counted.

_Corollary (any reachable, non-later-dated ancestor)._ Under the same
prerequisite, the argument generalizes beyond immediate parents and the selected
chain’s own tip transitions: `reach(X)` is a subset of `reach(Y)` whenever `X`
is reachable from `Y` through any sequence of parent edges—a pure fact about
DAGs, true regardless of which parent slot any edge occupies. If
`date(X) < date(Y)`, `version(Y) > version(X)` on the date segment alone; if
`date(X) = date(Y)`, then non-decreasing dates along the connecting path make
every commit on it share that date, so the calculated walk reaches `X` from `Y`,
`X`’s date cohort is contained in `Y`’s, and `Y` cannot be a member of `X`’s
cohort (no cycles), so `N(Y) > N(X)`. Either way, `version(Y) > version(X)`
whenever `X` is reachable from `Y` with a `date(X)` no later than `date(Y)`. The
prerequisite matters: a decreasing date buried along the connecting path can
hide an equal-dated ancestor behind a pruned older commit, in which case the
calculated counts carry no such ordering. (If `date(X) > date(Y)`, no guarantee
holds either; that is exactly the anomaly
[Canonical publication](#canonical-publication)’s continuity check must reject,
not silently trust.) This reachable, non-later-dated check is what licenses
Canonical publication’s continuity check; its numeric greater-than check remains
the guard that holds with no prerequisite at all.

These guarantees require committer dates to be non-decreasing along _every_
parent path reachable from a selected-chain commit, not only along the selected
chain itself: no commit may have a UTC committer date later than a commit that
can reach it. A reachable commit that violates this is a decreasing-history
error (exit code 1), detected while computing a date cohort; see
[Incomplete and partial histories](#incomplete-and-partial-histories). History
rewrites that change the selected chain, or that make any reachable commit’s
date decrease along a path that reaches it, invalidate these guarantees. Dirty
versions are explicitly outside the uniqueness and reversibility guarantees.

## Core calculation

### Committer date and UTC

GitCalVer MUST use the committer timestamp, not the author timestamp. Git stores
the timestamp as Unix epoch seconds plus a timezone offset; calculation MUST
interpret the instant in UTC and ignore the stored offset when choosing the
date.

The committer timestamp represents when a commit was applied to the branch and
changes under operations such as amend, rebase, and cherry-pick.

### First-parent traversal

Only first parents define the selected chain.

- A merge commit itself counts.
- Commits reachable only through a merge commit’s later parents do not count.
- A squash merge contributes the resulting commit.
- A fast-forward contributes each forwarded commit.

A target is a clean selected-chain member only if it is exactly present in that
first-parent traversal. Ordinary reachability is insufficient.

First-parent traversal determines selected-chain membership and branch selection
only. It does not bound `N`; the calculation algorithm below traverses all
parents to compute a target’s date cohort.

### Calculation algorithm

For a selected-chain target:

1. Convert the target’s committer timestamp to `YYYYMMDD` in UTC—this is the
   target’s own date; no traversal is required.
2. Traverse backward from the target through all parents, visiting each
   reachable commit at most once.
3. For each visited commit: if its UTC committer date equals the target’s, count
   it (including the target itself) and continue traversing its parents. If its
   date is strictly older, do not count it and do not traverse past it. If its
   date is strictly newer, reject the history.
4. Stop each path once pruned by an older date or once a true root is proved.
5. Let `N` be the total count from step 3.
6. Return `YYYYMMDD.N`.

An implementation MUST NOT return a result when local history ends before every
path’s older boundary or a true root can be proved.

<figure id="fig-3">
<svg role="img" width="480" height="260" viewBox="0 0 480 260" aria-labelledby="fig-3-title fig-3-desc" style="font-family:'IBM Plex Mono',monospace">
<title id="fig-3-title">Computing a target’s date cohort: count, prune, or reject</title>
<desc id="fig-3-desc">From target T, a same-date parent A1 is counted and explored further to A2. An older-dated parent B1 is a safe boundary: not counted, and nothing behind it needs to be visited. A later-dated parent C1 triggers rejection as a decreasing-history error.</desc>
<defs>
<marker id="fig2-arrow" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
<path d="M0,0 L10,5 L0,10 Z" fill="var(--border-strong)"/>
</marker>
<marker id="fig2-arrow-error" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
<path d="M0,0 L10,5 L0,10 Z" fill="var(--error)"/>
</marker>
</defs>
<path d="M256,98 L196,72" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig2-arrow)"/>
<path d="M138,60 L92,60" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig2-arrow)"/>
<path d="M304,98 L364,72" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig2-arrow)"/>
<path d="M422,60 L450,60" stroke="var(--border-strong)" stroke-width="1.5" stroke-dasharray="2 3" fill="none"/>
<path d="M280,126 L280,182" stroke="var(--error)" stroke-width="1.5" fill="none" marker-end="url(#fig2-arrow-error)"/>
<rect x="248" y="94" width="64" height="32" rx="6" fill="var(--count-soft)" stroke="var(--count)" stroke-width="1.5"/>
<text x="280" y="115" text-anchor="middle" font-size="13" fill="var(--text)">T</text>
<text x="280" y="140" text-anchor="middle" font-size="10" fill="var(--text-soft)">target</text>
<rect x="138" y="44" width="58" height="32" rx="6" fill="var(--count-soft)" stroke="var(--count)" stroke-width="1.5"/>
<text x="167" y="65" text-anchor="middle" font-size="13" fill="var(--text)">A1</text>
<text x="175" y="92" font-size="10" fill="var(--text-soft)">same date</text>
<rect x="28" y="44" width="58" height="32" rx="6" fill="var(--count-soft)" stroke="var(--count)" stroke-width="1.5"/>
<text x="57" y="65" text-anchor="middle" font-size="13" fill="var(--text)">A2</text>
<rect x="358" y="44" width="64" height="32" rx="6" fill="var(--bg-sunk)" stroke="var(--border)" stroke-width="1.5" stroke-dasharray="4 3"/>
<text x="390" y="65" text-anchor="middle" font-size="13" fill="var(--text-soft)">B1</text>
<text x="390" y="30" text-anchor="middle" font-size="10" fill="var(--text-soft)">older date—pruned</text>
<rect x="248" y="184" width="64" height="32" rx="6" fill="var(--bg-sunk)" stroke="var(--error)" stroke-width="2"/>
<text x="280" y="205" text-anchor="middle" font-size="13" fill="var(--text)">C1</text>
<text x="280" y="230" text-anchor="middle" font-size="10" fill="var(--error)">later date—rejected</text>
<rect x="10" y="236" width="14" height="14" fill="var(--count-soft)" stroke="var(--count)"/>
<text x="30" y="247" font-size="10.5" fill="var(--text-soft)">counted (same date)</text>
<rect x="180" y="236" width="14" height="14" fill="var(--bg-sunk)" stroke="var(--border)" stroke-dasharray="2 2"/>
<text x="200" y="247" font-size="10.5" fill="var(--text-soft)">pruned (older, safe)</text>
<rect x="340" y="236" width="14" height="14" fill="var(--bg-sunk)" stroke="var(--error)" stroke-width="2"/>
<text x="360" y="247" font-size="10.5" fill="var(--text-soft)">rejected (newer)</text>
</svg>
<figcaption>Figure 3. Computing target T’s date cohort. The walk follows every parent. A same-date ancestor (A1, A2) is counted and explored further. An older-dated ancestor (B1) is a safe boundary—not counted, and nothing behind it needs to be visited. A later-dated ancestor (C1) is not a boundary GitCalVer can trust: it means committer dates decreased somewhere in reachable history, so the whole calculation is rejected rather than silently under- or over-counting.</figcaption>
</figure>

## Selected branch

### Detection

Unless the caller explicitly selects a branch, implementations MUST use this
precedence:

1. The symbolic cached remote default at `refs/remotes/REMOTE/HEAD`.
2. Cached `REMOTE/main`, then `REMOTE/master`.
3. Local `main`, then local `master`.
4. Error if no branch can be selected.

`REMOTE` defaults to `origin` and MAY be selected explicitly. Remote selection
chooses cached refs only; it MUST NOT fetch.

After selecting the branch name, implementations MUST prefer its local branch
tip and otherwise use its cached remote-tracking tip. This keeps clean, unpushed
commits on the selected local branch calculable. The resolved tip MUST be cached
for the invocation so concurrent ref movement cannot combine two views of the
chain.

### Target relationship

Implementations MUST classify the target against the selected chain:

- **On-chain:** The target is an exact selected-chain member. Calculate it
  directly.
- **Off-chain but traceable:** The target can reach at least one selected-chain
  commit through any parent path. Use the newest such selected-chain commit as
  the calculation anchor and mark the result dirty.
- **Not traceable:** Complete local history proves that no common ancestry
  exists. Return the not-traceable result.
- **Unprovable:** Missing local history prevents membership, anchor, or
  nonrelationship proof. Return the incomplete-history result.

A commit reachable only through a merge’s second or later parent is off-chain,
not clean. An off-chain target that has merged a newer selected-chain commit
anchors at that newest reachable chain member.

## Targets and workspace state

With no supplied target, implementations MUST calculate `HEAD` and inspect the
current workspace when one exists. Staged changes, unstaged changes, and
untracked nonignored files make it dirty. Ignored files do not.

An explicit revision—including explicit `HEAD`—describes only that commit.
Implementations MUST ignore workspace state for explicit revisions. Its chain
relationship still applies, so an explicit off-chain revision remains dirty.

An explicitly supplied empty target is invalid; it is not equivalent to
omission. Bare repositories MUST support explicit revisions, reverse lookup, and
omitted-target calculation without attempting a workspace check.

## Dirty output

By default, implementations MUST refuse a dirty result. A caller MAY opt in with
a nonempty dirty string.

With a hash:

```text
PREFIXYYYYMMDD.NDIRTY.HASH
```

Without a hash:

```text
PREFIXYYYYMMDD.NDIRTY
```

`HASH` MUST be the first seven lowercase characters of the original target
commit’s full object ID. It MUST NOT use git’s repository-dependent abbreviation
machinery. The separator before `HASH` is exactly one literal period; there is
no `g` prefix.

A no-hash option is valid only when dirty output is enabled. An explicit
no-dirty option overrides dirty output configuration.

Dirty output is not uniquely reversible: different workspaces and different
off-chain histories can share the same dirty string.

## Deterministic interfaces

### Input precedence

A single positional target MAY be either a version or a revision. After removing
the exact configured prefix, an input with the complete shape `YYYYMMDD.N` MUST
enter reverse mode before revision parsing, even if a git ref has the same name
or the date is invalid. Trailing text and multiline values do not match.

A reverse input MUST include the exact configured prefix when that prefix is
nonempty. Implementations MUST validate the date as a Gregorian date and reject
counts with leading zeroes.

### Forward output

Forward calculation outputs the configured prefix, the base version, and any
permitted dirty suffix. An explicit revision ignores workspace state as defined
above.

### Reverse output

Reverse lookup walks the selected chain to find `YYYYMMDD`’s date block,
validating its older boundary or proving a true root, as in 0.2. Because `N` is
not dense, it MUST then compute each date-block member’s date cohort size to
find the one member (if any) whose `N` matches the requested count—`N` strictly
increases across the block, so this scan can stop as soon as it passes the
requested count. If no member matches, the implementation MUST report that the
version was not found. It MUST NOT return the nearest lower or higher commit.

### Exit codes

CLI implementations MUST use these exit codes:

| Code | Meaning                                                                  |
| ---- | ------------------------------------------------------------------------ |
| 0    | Success                                                                  |
| 1    | Invalid input, repository state, version date, or decreasing history     |
| 2    | Dirty workspace or off-chain target refused without dirty output enabled |
| 3    | Complete history proves the target is unrelated to the selected chain    |
| 4    | Local history cannot prove membership, anchor, or a complete date block  |

Library implementations SHOULD expose distinguishable errors with equivalent
semantics.

## Incomplete and partial histories

Core calculation and reverse lookup MUST NOT access the network, including lazy
fetches from partial-clone promisor remotes.

Shallow and partial histories MAY succeed when their local commit objects prove:

1. the target’s selected-chain relationship or reachable anchor; and
2. for `N`, that along every parent path from the target, the walk reaches
   either a true repository root or a commit whose UTC committer date is
   strictly older than the target’s, before it reaches any shallow boundary.

A shallow-boundary commit whose committer date is not strictly older than the
target’s leaves open the possibility that history beyond it shares the target’s
date. Implementations MUST then return the incomplete-history result (exit code
4), not a guessed version. A blanket shallow-repository rejection is not
conforming.

<figure id="fig-4">
<svg role="img" width="400" height="250" viewBox="0 0 400 250" aria-labelledby="fig-4-title fig-4-desc" style="font-family:'IBM Plex Mono',monospace">
<title id="fig-4-title">Whether a shallow clone can prove a target’s cohort</title>
<desc id="fig-4-desc">Top: every path from the target reaches either a same-date commit that keeps being explored, or a boundary strictly older than the target, so the count is provable. Bottom: a boundary that shares the target’s date leaves open whether hidden history beyond it shares the date too, so the implementation must refuse with the incomplete-history result.</desc>
<defs>
<marker id="fig4-arrow" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
<path d="M0,0 L10,5 L0,10 Z" fill="var(--border-strong)"/>
</marker>
</defs>
<text x="10" y="16" font-size="12" font-weight="600" fill="var(--text)">Provable—exit
code 0 permitted</text>
<path d="M78,55 L142,40" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig4-arrow)"/>
<path d="M78,65 L142,85" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig4-arrow)"/>
<path d="M178,40 L232,40" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig4-arrow)"/>
<rect x="22" y="45" width="56" height="30" rx="6" fill="var(--count-soft)" stroke="var(--count)" stroke-width="1.5"/>
<text x="50" y="65" text-anchor="middle" font-size="12" fill="var(--text)">T</text>
<rect x="142" y="25" width="56" height="30" rx="6" fill="var(--count-soft)" stroke="var(--count)" stroke-width="1.5"/>
<text x="170" y="45" text-anchor="middle" font-size="12" fill="var(--text)">A1</text>
<rect x="142" y="70" width="56" height="30" rx="6" fill="var(--bg-sunk)" stroke="var(--border)" stroke-width="1.5" stroke-dasharray="4 3"/>
<text x="170" y="90" text-anchor="middle" font-size="12" fill="var(--text-soft)">B1</text>
<rect x="232" y="25" width="130" height="30" rx="6" fill="var(--bg-sunk)" stroke="var(--count)" stroke-width="1.5" stroke-dasharray="4 3"/>
<text x="297" y="45" text-anchor="middle" font-size="10.5" fill="var(--text)">boundary, older</text>
<path d="M248,17 L253,24 L266,10" stroke="var(--count)" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
<text x="170" y="115" text-anchor="middle" font-size="10" fill="var(--text-soft)">every
path ends at a root or an older date</text>
<text x="10" y="150" font-size="12" font-weight="600" fill="var(--text)">Not
provable—exit code 4 required</text>
<path d="M78,190 L142,190" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig4-arrow)"/>
<path d="M178,190 L232,190" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig4-arrow)"/>
<rect x="22" y="175" width="56" height="30" rx="6" fill="var(--count-soft)" stroke="var(--count)" stroke-width="1.5"/>
<text x="50" y="195" text-anchor="middle" font-size="12" fill="var(--text)">T</text>
<rect x="142" y="175" width="56" height="30" rx="6" fill="var(--count-soft)" stroke="var(--count)" stroke-width="1.5"/>
<text x="170" y="195" text-anchor="middle" font-size="12" fill="var(--text)">A1</text>
<rect x="232" y="175" width="130" height="30" rx="6" fill="var(--bg-sunk)" stroke="var(--error)" stroke-width="2"/>
<text x="297" y="195" text-anchor="middle" font-size="10.5" fill="var(--text)">boundary, same date</text>
<path d="M370,180 L384,194 M384,180 L370,194" stroke="var(--error)" stroke-width="2" fill="none" stroke-linecap="round"/>
<text x="170" y="225" text-anchor="middle" font-size="10" fill="var(--error)">boundary shares the target’s date—unproven</text></svg>
<figcaption>Figure 4. Whether a shallow or partial clone can prove a target’s count. Top: every path’s shallow boundary has a date strictly older than the target’s—no hidden commit beyond it can share the target’s date, so the count is provable from local history alone. Bottom: a shallow boundary shares the target’s date—history beyond it might still contribute to the cohort, so the implementation MUST refuse with the incomplete-history result (exit code 4) rather than guess.</figcaption>
</figure>

Implementations MUST ignore replacement refs while calculating. They MUST reject
a repository with a legacy `info/grafts` file as incomplete because grafts
silently replace stored ancestry.

Conformance requires SHA-1 repositories. Implementations MAY support SHA-256
repositories and SHOULD report that capability separately.

Because `N` now depends on every reachable parent, a shallow clone safe for 0.2
is not necessarily safe for 0.3: it must include every commit committed on the
target’s UTC date that is reachable from the target across all merged branches,
not merely the selected chain. `--shallow-since=<day-before-target-date>` is
insufficient when a same-day branch was merged from deeper history. A partial
clone with `--filter=blob:none` and full commit history remains sufficient,
since only commit objects are required.

## Canonical publication

Core calculation does not coordinate independent clones. Two offline clones can
temporarily calculate the same candidate when their local histories diverge.
Canonical publication resolves that collision by claiming an immutable,
protected tag without force after CI succeeds.

Publication is a separate, explicit operation and MAY access the network. For
one caller-defined canonical tag namespace, it MUST:

1. Refresh the selected remote branch and require the publishing commit to be
   its current tip.
2. Enumerate all globally published matching tags, including tags whose commits
   are otherwise unreachable from the selected tip. A registry release remains
   part of publication history even if it is later yanked.
3. Select the numerically greatest matching GitCalVer version.
4. Validate that tag’s date segment against its target commit.
5. Require its target to remain reachable from the publishing commit through any
   parent, with a committer date no later than the publishing commit’s own.
6. Require the new candidate to be greater, unless the candidate tag already
   points to the publishing commit.
7. Create the candidate tag without force.

This two-part check—reachable through any parent, with a non-later committer
date—guarantees the new candidate’s version is greater than the previous tag’s
whenever the [Guarantees](#guarantees-and-prerequisites) prerequisite holds, per
the corollary above; step 6’s numeric comparison remains the guard that holds
with no prerequisite at all, so the two steps are complementary, not redundant.
The continuity check replaces first-parent ancestry, which was never
load-bearing for this guarantee—it was simply the ancestry notion 0.2’s own `N`
used, carried into publication for consistency. Requiring first-parent ancestry
would incorrectly and permanently block publication after a
merge-then-fast-forward reparents the previous canonical tag’s target behind a
different first parent, even though it remains reachable with an unchanged date
and the guarantee still holds. This does not relax protection against publishing
an unrelated or rogue release line: step 1 already restricts the publishing
commit to the selected branch’s current remote tip, and step 3 selects the
numerically greatest matching tag across the entire namespace regardless of
reachability, so a genuinely newer tag on a different line still blocks
publication as a newer-published-tag failure, not a continuity failure. A
previous tag’s target that is unreachable—because history was rewritten,
rebased, or replaced rather than merely reparented by a fast-forward—still fails
this check and blocks publication as a continuity failure, exactly as it did
under first-parent ancestry; so does a previous tag’s target that is reachable
but carries a later committer date than the publishing commit, a case
first-parent ancestry could not even express.

No other publication step needs to change. Steps 1–4 and 6–7 depend on the
remote tip, the global tag namespace, and version-string comparison—none of
which involve an ancestry notion—so 0.2’s wrong-branch and overwrite protections
carry over unchanged; only step 5’s ancestry test was coupled to `N`’s old,
now-superseded definition.

<figure id="fig-5" tabindex="0" aria-describedby="horizontal-scroll-instructions" data-horizontal-scroll="true">
<svg role="img" width="580" height="330" viewBox="0 0 580 330" aria-labelledby="fig-5-title fig-5-desc" style="font-family:'IBM Plex Mono',monospace">
<title id="fig-5-title">Publication continuity under the reparenting topology</title>
<desc id="fig-5-desc">The same graph as Figure 2: the previous canonical tag’s target C4 is no longer the publishing commit M’s first-parent ancestor, but it is still reachable through M’s second parent with an unchanged date, so step 5 passes. A reachable target with a later date than the publishing commit still fails the check.</desc>
<defs>
<marker id="fig5-arrow" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
<path d="M0,0 L10,5 L0,10 Z" fill="var(--border-strong)"/>
</marker>
<marker id="fig5-arrow-error" viewBox="0 0 10 10" refX="8" refY="5" markerWidth="6" markerHeight="6" orient="auto">
<path d="M0,0 L10,5 L0,10 Z" fill="var(--error)"/>
</marker>
</defs>
<path d="M110,55 L80,55" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig5-arrow)"/>
<path d="M200,55 L170,55" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig5-arrow)"/>
<path d="M290,55 L260,55" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig5-arrow)"/>
<path d="M200,145 L170,145" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig5-arrow)"/>
<path d="M110,145 L95,145" stroke="var(--border-strong)" stroke-width="1.5" stroke-dasharray="2 3" fill="none"/>
<text x="72" y="149" font-size="14" fill="var(--text-soft)">...</text>
<path d="M380,95 L250,124" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig5-arrow)"/>
<path d="M410,79 L320,66" stroke="var(--count)" stroke-width="2" fill="none" marker-end="url(#fig5-arrow)"/>
<text x="252" y="118" font-size="10.5" fill="var(--text-soft)">1st parent</text>
<text x="345" y="76" font-size="10.5" fill="var(--count)">2nd parent</text>
<rect x="20" y="34" width="60" height="32" rx="6" fill="var(--bg-sunk)" stroke="var(--border-strong)" stroke-width="1.5"/>
<text x="50" y="55" text-anchor="middle" font-size="13" fill="var(--text)">C1</text>
<rect x="110" y="34" width="60" height="32" rx="6" fill="var(--bg-sunk)" stroke="var(--border-strong)" stroke-width="1.5"/>
<text x="140" y="55" text-anchor="middle" font-size="13" fill="var(--text)">C2</text>
<rect x="200" y="34" width="60" height="32" rx="6" fill="var(--bg-sunk)" stroke="var(--border-strong)" stroke-width="1.5"/>
<text x="230" y="55" text-anchor="middle" font-size="13" fill="var(--text)">C3</text>
<rect x="290" y="34" width="60" height="32" rx="6" fill="var(--count-soft)" stroke="var(--count)" stroke-width="2"/>
<text x="320" y="55" text-anchor="middle" font-size="13" fill="var(--text)">C4</text>
<rect x="110" y="124" width="60" height="32" rx="6" fill="var(--bg-sunk)" stroke="var(--border-strong)" stroke-width="1.5"/>
<text x="140" y="145" text-anchor="middle" font-size="13" fill="var(--text)">F1</text>
<rect x="200" y="124" width="60" height="32" rx="6" fill="var(--bg-sunk)" stroke="var(--border-strong)" stroke-width="1.5"/>
<text x="230" y="145" text-anchor="middle" font-size="13" fill="var(--text)">F2</text>
<rect x="380" y="79" width="60" height="32" rx="6" fill="var(--count-soft)" stroke="var(--count)" stroke-width="2"/>
<text x="410" y="100" text-anchor="middle" font-size="13" fill="var(--text)">M</text>
<text x="16" y="180" font-size="11.5" fill="var(--text-soft)">C4 = target of the
previously published canonical tag · M = publishing commit</text>
<path d="M16,193 L21,200 L34,186" stroke="var(--count)" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round"/>
<text x="42" y="202" font-size="12.5" fill="var(--text)">M reaches C4 (2nd parent, non-later date): step 5&#160;<tspan fill="var(--count)" font-weight="600">passes</tspan>.</text>
<text x="16" y="240" font-size="13" font-weight="600" fill="var(--text)">Contrast
— a target that fails step 5</text>
<path d="M108,270 L172,270" stroke="var(--border-strong)" stroke-width="1.5" fill="none" marker-end="url(#fig5-arrow-error)"/>
<rect x="50" y="255" width="58" height="30" rx="6" fill="var(--count-soft)" stroke="var(--count)" stroke-width="1.5"/>
<text x="79" y="275" text-anchor="middle" font-size="12" fill="var(--text)">M</text>
<rect x="172" y="255" width="58" height="30" rx="6" fill="var(--bg-sunk)" stroke="var(--error)" stroke-width="2"/>
<text x="201" y="275" text-anchor="middle" font-size="12" fill="var(--text)">D1</text>
<path d="M242,262 L256,276 M256,262 L242,276" stroke="var(--error)" stroke-width="2" fill="none" stroke-linecap="round"/>
<text x="266" y="275" font-size="11" fill="var(--error)">later date than M,
still reachable</text>
<text x="16" y="312" font-size="12.5" fill="var(--text)">D1 is later-dated than M: step 5&#160;<tspan fill="var(--error)" font-weight="600">fails</tspan>&#160;despite being reachable.</text></svg>
<figcaption>Figure 5. Canonical publication’s continuity check under the same reparenting topology as Figure 2. The previous canonical tag’s target (C4) is no longer M’s first-parent ancestor, but it is still reachable through M’s second parent with an unchanged date—so 0.3’s step 5 accepts it, where 0.2’s first-parent-only check would have blocked publication indefinitely. A reachable target with a later date than the publishing commit (D1) still fails the check: reachability alone was never sufficient.</figcaption>
</figure>

A retry is idempotent only when the matching canonical tag points to the same
commit and that commit remains the selected remote tip. A matching tag at a
different commit, a newer published tag, a continuity failure, or a lost race
MUST block publication.

Tag namespaces MUST be configured as immutable before automated publication.
Publication workflows SHOULD use constant concurrency so only one claimant runs
at a time; nonforce tag creation remains the final collision guard.

## Migration from 0.2

Upgrading an implementation from 0.2 to 0.3 changes the meaning of previously
computed version strings for any target whose date cohort includes a merge that
brought in other same-date commits. A version string computed under 0.2 MAY name
a different commit—or no commit—once recomputed under 0.3. This is a one-time
cost of fixing the unsound guarantee recorded in 0.2’s erratum. 0.3 also relaxes
canonical publication’s tag-continuity check (step 5) from first-parent ancestry
to reachability through any parent with a non-later committer date: 0.2’s
stricter, first-parent-only check does not merely fail to help with the
reparenting defect, it actively perpetuates it—a canonical tag namespace that
hit the reparenting topology would stay permanently blocked from all further
publication even after upgrading to 0.3’s counting rule, because the previous
release’s target keeps failing a first-parent-ancestry test forever. Callers
running affected namespaces MUST upgrade publication tooling together with
calculation tooling; the counting fix alone does not unblock them. Canonical
publication’s greater-than check (step 6) is unchanged and still prevents any
0.3 candidate from overwriting an already-published 0.2-era tag at a different
commit; callers SHOULD still audit any locally cached, unpublished 0.2 version
strings before relying on them further.

## Compatibility

The base algorithm is ecosystem-independent. Prefixes, local dirty forms,
dependency ranges, and constrained platform fields are nonnormative integration
choices documented on the [Compatibility](/compatibility) page.
